//import store from '../index.js';

const state = {
    list:[],
    hasMorePage: false,
    isEmpty: false,
    loading: true
};

const mutations = {
};

const actions = {
};

const userList = {
    state,
    mutations,
    actions
};
export default userList;